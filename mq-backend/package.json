{
  "name": "mq-backend",
  "version": "1.0.0",
  "description": "Backend API for the Kafka demo app",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "build-img": "rm -rf client_build && pushd ../mq-app && npm run build && popd && cp -r ../mq-app/build client_build && docker build -t mq-backend ."
  },
  "author": "captainGeech42",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "express": "^4.17.1",
    "express-ws": "^5.0.2"
  }
}
